# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/
$ ->
  # if $("#formation_seance_ids").val()
  #   nb = $("#formation_seance_ids").val()
  #   # console.log nb
  #   cpt = 0
  #   # $('form a.add_nested_fields seance').on 'click', nestedFormEvents.addFields
  #   while cpt < nb
  #     $('form a.add_nested_fields seance').trigger('click')
  #     cpt++
